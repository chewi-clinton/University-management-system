Add reusable pieces (headers, footers, cards) here as HTML files. I'll convert them into React components (JSX) when you add them and ask me to convert.

Example:
- `header.html` → `Header.jsx`
- `invoice-card.html` → `InvoiceCard.jsx`
